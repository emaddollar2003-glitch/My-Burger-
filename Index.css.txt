/* إعدادات عامة وتصفير المسافات */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
    font-family: 'Cairo', sans-serif;
}

body {
    background-color: #0d0a07; /* خلفية داكنة مائلة للقهوة والرماد الفخم */
    color: #ffffff;
    overflow-x: hidden;
}

/* خط الفصل الشفاف بين الأقسام */
.separator {
    border: 0;
    height: 1px;
    background: rgba(255, 255, 255, 0.08);
    max-width: 1200px;
    margin: 0 auto;
}

/* القائمة العلوية - Header */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    padding: 25px 20px;
}

header .logo {
    font-size: 1.6rem;
    font-weight: bold;
    color: #ff9f1c; /* لون برتقالي دافئ يفتح النفس */
}

header nav ul {
    display: flex;
    list-style: none;
    gap: 35px;
}

header nav ul li a {
    color: #e2e8f0;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.05rem;
    transition: color 0.3s ease;
}

header nav ul li a:hover {
    color: #ff9f1c;
}

/* القسم الرئيسي - Hero */
.hero-section {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 60px auto;
    padding: 20px;
    gap: 50px;
}

.hero-text h3 {
    color: #ff9f1c;
    font-size: 1.3rem;
    margin-bottom: 15px;
}

.hero-text h1 {
    font-size: 3.5rem;
    line-height: 1.3;
    margin-bottom: 15px;
}

.hero-text h2 {
    color: #94a3b8;
    font-size: 1.4rem;
    font-weight: normal;
    margin-bottom: 35px;
}

.hero-buttons {
    display: flex;
    gap: 20px;
}

.btn-primary, .btn-secondary {
    padding: 14px 32px;
    border-radius: 30px;
    text-decoration: none;
    font-weight: bold;
    transition: all 0.3s ease;
}

.btn-primary {
    background-color: #ff9f1c;
    color: #0d0a07;
}

.btn-primary:hover {
    background-color: #e78607;
    box-shadow: 0 0 20px rgba(255, 159, 28, 0.4);
}

.btn-secondary {
    background-color: rgba(255, 255, 255, 0.03);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(8px);
}

.btn-secondary:hover {
    background-color: rgba(255, 255, 255, 0.08);
}

.hero-img img {
    border-radius: 30px;
    max-width: 100%;
    height: auto;
    box-shadow: 0 20px 40px rgba(0,0,0,0.5);
}

/* قسم المنيو - Menu Section */
.works-section {
    max-width: 1200px;
    margin: 120px auto;
    padding: 20px;
}

.works-section h2, .about-section h2, .contact-section h2 {
    font-size: 2.8rem;
    text-align: center;
    margin-bottom: 10px;
}

.works-section h2 span, .about-section h2 span, .contact-section h2 span {
    color: #ff9f1c;
}

.section-desc {
    text-align: center;
    color: #94a3b8;
    font-size: 1.1rem;
    margin-bottom: 50px;
}

.works-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 30px;
}

.work-card {
    background-color: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.04);
    border-radius: 20px;
    padding: 20px;
    flex: 1 1 calc(33.333% - 20px);
    text-align: center;
    transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow 0.4s ease;
}

.card-img-container {
    width: 100%;
    height: 220px;
    overflow: hidden;
    border-radius: 15px;
    margin-bottom: 20px;
}

.work-card img {
    width: 100%;
    height: 100%;
    object-fit: crop;
    transition: transform 0.4s ease;
}

.work-card h3 {
    font-size: 1.4rem;
    margin-bottom: 10px;
}

.item-details {
    color: #94a3b8;
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 20px;
    height: 70px; /* يحافظ على تناسق الارتفاع */
}

.price {
    display: block;
    font-size: 1.3rem;
    font-weight: bold;
    color: #ff9f1c;
}

/* تأثير تحريك الكارت عند تمرير الماوس مثل الفيديو */
.work-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
    border-color: rgba(255, 159, 28, 0.2);
}

.work-card:hover img {
    transform: scale(1.08); /* تكبير الصورة داخل الكارد سلاسة */
}

/* قسم عن المطعم - About المطعم */
.about-section {
    display: flex;
    align-items: center;
    max-width: 1200px;
    margin: 120px auto;
    padding: 20px;
    gap: 80px;
}

.about-section h2 {
    text-align: right;
    margin-bottom: 25px;
}

.about-text p {
    color: #cbd5e1;
    font-size: 1.15rem;
    line-height: 1.9;
}

.about-img img {
    border-radius: 20px;
    max-width: 100%;
}

/* قسم التواصل والحجز */
.contact-section {
    max-width: 1200px;
    margin: 120px auto;
    padding: 40px 20px;
    text-align: center;
}

.contact-info {
    margin: 40px 0;
    font-size: 1.25rem;
    color: #e2e8f0;
}

.contact-info p {
    margin: 15px 0;
}

.social-links {
    display: flex;
    justify-content: center;
    gap: 25px;
}

.social-links a {
    color: #ff9f1c;
    text-decoration: none;
    border: 1px solid rgba(255, 159, 28, 0.3);
    padding: 10px 25px;
    border-radius: 30px;
    font-weight: bold;
    background: rgba(255, 159, 28, 0.02);
    transition: all 0.3s ease;
}

.social-links a:hover {
    background-color: #ff9f1c;
    color: #0d0a07;
    box-shadow: 0 5px 15px rgba(255, 159, 28, 0.3);
}

/* الفوتر - Footer */
footer {
    text-align: center;
    padding: 40px 20px;
    background-color: #060504;
    color: #57534e;
    font-size: 0.95rem;
    border-top: 1px solid rgba(255, 255, 255, 0.02);
}

/* ----------------------------------------------- */
/* شاشات الجوال والتجاوب (Responsive Design) */
/* ----------------------------------------------- */
@media (max-width: 968px) {
    header {
        flex-direction: column;
        gap: 20px;
    }

    .hero-section {
        flex-direction: column-reverse;
        text-align: center;
    }

    .about-section {
        flex-direction: column;
        text-align: center;
    }

    .about-section h2 {
        text-align: center;
    }

    .hero-text h1 {
        font-size: 2.6rem;
    }

    .hero-buttons {
        justify-content: center;
    }

    .work-card {
        flex: 1 1 calc(50% - 15px); /* عرض طبقين في الصف للتابلت */
    }
}

@media (max-width: 576px) {
    .work-card {
        flex: 1 1 100%; /* طبق واحد كامل في الصف للجوال */
    }

    .hero-buttons {
        flex-direction: column;
        gap: 12px;
    }
    
    .works-section h2, .about-section h2, .contact-section h2 {
        font-size: 2.2rem;
    }
}
